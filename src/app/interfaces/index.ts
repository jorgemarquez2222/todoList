export interface Task{
    id: string
    name: string
    editando: boolean
}

export interface Product {
    id:          number;
    title:       string;
    price:       number;
    description: string;
    image:      string[];
  }